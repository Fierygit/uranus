初始化:
	对每一个part进行连接请求
	将所有的part日志同步 -> 选择一个合理的策略
	// 在基本版中, 选择index更小的日志进行同步. 因为不会有参与者出错
	// 在高级版中, 由于参与者可能失败且参与者失败了就移除


执行命令:
	每次从queue中取出一个命令
	向所有part发起第一阶段请求
	if 所有都ok:
		发起2 phase
		if 所有都ok:
			结束
		else:
			abort
			发起恢复
	else:
		abort



客户端交互:
	不断读取客户端命令
	加入到queue


1、 p 挂了：
    c： 此时在处理，

